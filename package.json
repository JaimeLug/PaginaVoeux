{
  "name": "pagina-voeux",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "thumbs": "node scripts/generate-thumbs.js",
    "build": "npm run thumbs",
    "serve": "npx http-server -p 8080"
  },
  "devDependencies": {
    "sharp": "^0.33.4"
  }
}